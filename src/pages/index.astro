---
import { Image } from "astro:assets";
import Layout from "@/layouts/Layout.astro";

// const res = await fetch("https://kapilab.sumika-creative.net/wp-json/wp/v2/", {
//   headers: {
//     "Cache-Control": "no-cache"
//   }
// });
const USER = "kapilabpass";
const APP_PASSWORD = "YdbV RoaF VBC4 SoBH iVxU SiaO";

const token = Buffer.from(`${USER}:${APP_PASSWORD}`).toString("base64");

const res = await fetch("https://kapilab.sumika-creative.net/wp-json/wp/v2/posts", {
  headers: {
    "Authorization": `Basic ${token}`,
    "Cache-Control": "no-cache",
    "User-Agent": "AstroWorker/1.0",
  },
});


---


<Layout>
	<main class="pt-[64px] lg:pt-[100px]">
		<div class="container mx-auto px-5 lg:px-10 md:py-14 py-8">
			<h1>テスト</h1>
			{
				posts.map((post) => (
					<h2 set:html={post.title.rendered} />
					<p set:html={post.content.rendered} />
				))
			}
		</div>
	</main>
</Layout>
